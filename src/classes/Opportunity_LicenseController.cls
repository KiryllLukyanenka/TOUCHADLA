public with sharing class Opportunity_LicenseController extends AbstractAppDController{
    public Opportunity_LicenseController(ApexPages.StandardController stdController){
        super(stdController);
    }
}